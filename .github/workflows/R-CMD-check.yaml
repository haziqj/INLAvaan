# Workflow derived from https://github.com/r-lib/actions/tree/v2/examples
# Need help debugging build failures? Start at https://github.com/r-lib/actions#where-to-find-help
on:
  push:
    branches: [main, master]
  pull_request:

name: R-CMD-check

permissions: read-all

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  R-CMD-check:
    runs-on: ${{ matrix.config.os }}

    name: ${{ matrix.config.os }} (${{ matrix.config.r }}) ${{ matrix.config.label }}

    strategy:
      fail-fast: false
      matrix:
        config:
          # Minimal CRAN coverage. R-devel goes on Windows (CRAN's incoming
          # checks run win-devel, and CRAN ships r-devel Windows binaries so
          # blavaan/rstan need no compilation; Linux devel would build them
          # from source).
          - {os: macos-latest,   r: 'release'}
          - {os: windows-latest, r: 'devel'}
          - {os: ubuntu-latest,  r: 'release'}
          # lavaan development version from GitHub (the 0.6 -> 0.7 transition
          # broke many internals, so track upstream). blavaan is removed in
          # this job (see the install step below): its binary is built against
          # CRAN's current lavaan and carries stale copies of lavaan S4
          # methods that break under any other lavaan version. Suggests are
          # therefore not forced and vignettes (which use blavaan) are
          # skipped.
          - {
              os: ubuntu-latest,
              r: 'release',
              label: '(lavaan devel)',
              lavaan: 'devel',
              force_suggests: 'false',
              build_args: 'c("--no-manual", "--no-build-vignettes")',
              check_args: 'c("--no-manual", "--as-cran", "--ignore-vignettes")'
            }

    env:
      GITHUB_PAT: ${{ secrets.GITHUB_TOKEN }}
      R_KEEP_PKG_SOURCE: yes
      _R_CHECK_FORCE_SUGGESTS_: ${{ matrix.config.force_suggests || 'true' }}

    steps:
      - uses: actions/checkout@v5

      - uses: r-lib/actions/setup-r@v2
        with:
          r-version: ${{ matrix.config.r }}
          use-public-rspm: true

      # Quarto (needed for the vignettes, which bundles its own pandoc) is
      # installed automatically because the repository contains .qmd files.
      - uses: r-lib/actions/setup-r-dependencies@v2
        with:
          extra-packages: any::rcmdcheck
          needs: check

      - name: Install lavaan devel from GitHub
        if: matrix.config.lavaan == 'devel'
        run: |
          try(remove.packages("blavaan"), silent = TRUE)
          pak::pak("yrosseel/lavaan")
        shell: Rscript {0}

      - uses: r-lib/actions/check-r-package@v2
        with:
          upload-snapshots: true
          build_args: ${{ matrix.config.build_args || 'c("--no-manual")' }}
          args: ${{ matrix.config.check_args || 'c("--no-manual", "--as-cran")' }}
          error-on: '"warning"'
